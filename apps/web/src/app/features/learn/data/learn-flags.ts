/**
 * Temporary: open every Learn unit regardless of progress (mirrors the API's
 * `Learn:UnlockAll` setting in services/learning-api/appsettings.json). Set both
 * to false to restore the sequential path where each unit unlocks when the
 * previous one is complete.
 */
export const LEARN_UNLOCK_ALL = true;
